/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['control.mayway.in'],
    //domains: ['localhost'],
  },
}



module.exports = nextConfig
